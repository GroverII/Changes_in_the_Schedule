import { Point } from './interface';
export declare const getStartPoint: (width: number, circleWidth: number, borderWidth: number) => Point;
export declare const getCenter: (width: number, borderWidth: number) => {
    x: number;
    y: number;
};
export declare const radianToAngle: (radian: number) => number;
